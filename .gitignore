# dependencies
node_modules/
.pnp
.pnp.js

# build
.next/
out/
dist/
build/

# env & secrets — ห้าม commit เด็ดขาด
.env
.env.*
!.env.example
*credentials*.json
*service-account*.json

# ไฟล์ต้นฉบับรายเดือน — ใหญ่เกินและไม่ใช่ซอร์ส
uploads/
*.pptx
*.xlsx

# system
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# next
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# รูปประจำเดือน — ไฟล์ใหญ่ ไม่เก็บใน git · แต่คู่มือในโฟลเดอร์ต้องติดไปด้วย
# อย่าเขียนเป็น photos/ เฉย ๆ เพราะ git จะไม่เข้าไปในโฟลเดอร์ที่ถูกตัดทั้งอัน
# แล้วบรรทัด ! ข้างล่างจะไม่มีผล
/public/photos/*
!/public/photos/README.md
