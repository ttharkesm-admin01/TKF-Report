# CLAUDE.md

> **อ่าน `docs/PROJECT.md` ให้จบก่อนเริ่มงานทุกครั้ง** ไฟล์นี้เก็บเฉพาะสิ่งที่ต้องรู้ทันที
> รายละเอียดทั้งหมดอยู่ใน `docs/PROJECT.md` — ห้ามคัดลอกเนื้อหามาซ้ำที่นี่

## โปรเจกต์นี้คืออะไร

ระบบทำรายงานประจำเดือนของ **แผนกบริการสำนักงาน โรงงานผลิตอาหารสัตว์ธารเกษม (CPF)**
แทนการก๊อปวางเข้า PowerPoint ทุกเดือน · ปลายทางคือ **นำเสนอบนเว็บ (Next.js)** พิมพ์ PDF จากหน้าเดิม

**ขอบเขตเฟสแรก: หมวด 2.x เท่านั้น** (13 หัวข้อ) อีก 3 หมวดเป็นของหน่วยงานอื่น

## สถานะปัจจุบัน

**ทำครบทุกชนิดบล็อกแล้ว** — เว็บ Next.js เปิดดูได้ครบทั้งเล่ม
`cover` `closing` `monthly-matrix` `chart` `list-table` `photo-grid` `photo-set` `scan` `text`
เรนเดอร์เต็มรูปแบบทั้งหมด
ที่ยังไม่มีคือ **ข้อมูล** ไม่ใช่โค้ด — `list-table` 6 อันรอ `columns` อีก 1 อันมีคอลัมน์แต่ยังไม่มีแถว · บล็อกรูปรอไฟล์รูป
ตัวเลขทุกตัวผ่าน `lib/matrix.ts` · ไม่มีการคำนวณสด ๆ ในคอมโพเนนต์

| ไฟล์ | หน้าที่ |
|---|---|
| `config/sections.json` | หัวใจของระบบ · 13 หัวข้อ + ข้อมูลจริงถึง ก.ค. 2569 |
| `config/round.json` | เดือน/ปีของรอบที่ทำอยู่ · แก้ที่เดียวทั้งเล่มขยับ |
| `shared/schema.ts` | type ของบล็อก + ตารางหน่วย 10 หน่วย + ตัวช่วยคำนวณ |
| `lib/deck.ts` | แปลง config เป็นลำดับสไลด์ · **จุดเดียวที่สร้างเลขหัวข้อ** · หั่นตารางยาวเป็นหลายหน้า |
| `lib/matrix.ts` | **จุดเดียวที่คำนวณตัวเลข** · ยอดรวม เฉลี่ย ราคาถ่วงน้ำหนัก บรรทัดสรุป ชุดข้อมูลกราฟ |
| `lib/photos.ts` + `config/photos.json` | รูปของแต่ละบล็อก · `photos.json` สร้างด้วยสคริปต์ ห้ามแก้มือ |
| `arrange.json` ในโฟลเดอร์รูป | ลำดับ/ซ่อน/คำบรรยายที่จัดเอง · หน้า `/arrange` สร้างให้ |
| `lib/resize.ts` | ย่อรูปในเบราว์เซอร์ก่อนส่ง · ลอง JPEG กับ PNG แล้วเลือกอันเล็กกว่า |
| `lib/github.ts` + `config/repo.json` | commit เข้ารีโปตรงจากเบราว์เซอร์ผ่าน Git Data API (commit เดียวได้หลายร้อยไฟล์) · `readJsonFile()` อ่านของสดก่อนเขียน |
| `lib/useToken.ts` | โทเคน GitHub ที่ทุกหน้าใช้ร่วมกัน · พิมพ์ที่ `CommitPanel` แล้วหน้าที่ครอบอยู่ดึงของสดได้ทันที |
| `docs/source/tables-2569-07.md` | ตาราง 24 อันที่ดึงจากเด็ค 1.x/3.x/4.x · ข้อมูลดิบไว้อ้างอิง ยังไม่เข้าระบบ |
| `scripts/scan-photos.mjs` | อ่าน `public/photos/<รอบ>/<รหัสบล็อก>/` แล้วเขียน `config/photos.json` |
| `scripts/check-overflow.mjs` | จับสไลด์ที่เนื้อหาล้นกรอบ — สไลด์ตัดเนื้อหาเงียบ ๆ ไม่มี error |
| `components/slide/` | ตัวเรนเดอร์สไลด์แต่ละชนิด |
| `lib/entry.ts` + `lib/jsonFormat.ts` | รวมช่องที่ต้องกรอก **ทั้ง 12 เดือน** · นับว่าเดือนไหนยังค้าง · เขียน JSON กลับให้ diff เหลือบรรทัดเดียว |
| `lib/text.ts` | ตัดข้อความเป็นย่อหน้า/หัวข้อย่อย · **จุดเดียวที่ตัดสินว่าข้อความยาวเกินสไลด์ไหม** |
| `lib/structure.ts` | เพิ่ม/ลบ/สลับ/แก้บล็อกบน config ดิบ · ทุกฟังก์ชันคืนก้อนใหม่ ไม่แก้ของเดิม |
| `app/` | `/` หน้าแรก (การ์ดงานของรอบ + สารบัญ) · `/deck` ทั้งเล่ม+PDF · `/present` นำเสนอ · `/edit` กรอกตัวเลข · `/arrange` ลงรูป · `/structure` แก้โครงสร้าง |
| `components/nav/SiteNav.tsx` | เมนูบนสุดของทุกหน้าที่ไม่ใช่เวทีสไลด์ · สูง `h-12` คงที่ (แถบเครื่องมือ `/edit` ยึด `top-12` ตามนี้) |

ตัวเลขจริงตอนนี้: **57 บล็อก · เรนเดอร์เต็มทุกอัน** · เด็คออกมา **60 หน้า**
(57 บล็อก + ปกเล่ม + ตาราง OE 21 แถวถูกหั่นเป็น 3 หน้า) ยังไม่มีรูปสักใบ
หน้ากระดาษ 960 × 540 pt เท่าสไลด์ PowerPoint จอกว้างพอดี · กริดรูปเกิน 105 ใบขึ้นหน้าใหม่เอง

**ผู้ใช้ทำครบวงจรเองได้แล้วโดยไม่ต้องผ่านเรา** — กรอกตัวเลขที่ `/edit` ลงรูปที่ `/arrange`
ทั้งสองหน้า commit เข้ารีโปตรงจากเบราว์เซอร์ แล้ว Actions build ใหม่เอง
`/edit` **กรอกได้ทั้ง 12 เดือน ไม่ใช่แค่เดือนของรอบ** — มีสองโหมด
`เดือนเดียว` (งานประจำรอบ มีค่าเดือนก่อนให้เทียบ) กับ `ทั้งปี` (ตาราง แถว × 12 เดือน ตามเก็บของที่ตกไป)
แถบเดือนบนหัวหน้าบอกว่าเดือนไหนกรอกไปกี่ช่องแล้ว และเดือนไหนยังค้าง · คลิกเพื่อสลับเดือน
ช่องกรอกเดินด้วยลูกศร/Enter แบบสเปรดชีต · `Esc` คืนค่าเดิม · พิมพ์ `-` = ไม่มีรายการ
คีย์ของช่องคือ `บล็อก.แถว.เดือน` และงานค้างใน localStorage เก็บทั้งปีในกุญแจเดียว (`entry:<ปี>`)
`/edit` **เพิ่มแถวใหม่เข้าตารางได้เองจากหน้าเว็บ** (ปุ่ม `+ เพิ่มรายการ` ท้ายทุกตาราง)
และมีกล่องพิมพ์ **ข้อความสรุป** ของบล็อก `text` ท้ายหน้า ส่งไปในคอมมิตเดียวกับตัวเลข
คีย์ตั้งให้เป็น `custom-N` · ค่าเริ่มต้นเป็น 12 ช่องว่าง · ลบได้เฉพาะแถวที่ยังไม่ได้ส่ง

**หน้า `/structure` ทำเสร็จแล้ว** — ผู้ใช้แก้โครงสร้างเด็คเองได้จากเบราว์เซอร์
เพิ่มตาราง · ลบตาราง (เตือนก่อนถ้ามีข้อมูล) · เปลี่ยนชื่อ · สลับลำดับบล็อก ·
กำหนดคอลัมน์และแถวของ `list-table` ได้ครบ (ชนิดคอลัมน์ ความกว้าง รวมช่อง แถวต่อหน้า)
**ปลดล็อก 6 บล็อก `list-table` ที่ไม่มี `columns` แล้ว — ไม่ต้องรอไฟล์ 2.x อีก**
ตาราง 12 เดือนที่สร้างจากหน้านี้เกิดมาไม่มีแถว · `/edit` แสดงบล็อกเปล่าด้วย
จึงกด `+ เพิ่มรายการ` ใส่แถวแรกได้ ไม่งั้นตารางที่สร้างใหม่จะตายตั้งแต่เกิด

หน้านี้เขียนทั้งไฟล์ ไม่ใช่แค่ช่องเดียว จึง**เทียบของสดตอนกดส่ง ถ้ามีคนแก้แทรกจะหยุดและให้โหลดใหม่**
(หน้า `/edit` ทาบเฉพาะช่องจึงไม่ต้องหยุด)

**งานถัดไป: ต่อ AI เขียนบรรทัดสรุป** (ไม่เร่ง — `summarySentence()` ใช้ได้อยู่แล้ว)
ลำดับขั้นทั้งหมดอยู่ใน `docs/PROJECT.md` ข้อ 8

**ขั้นที่ 7 `validate` ยกเลิกแล้ว** — ใช้กันเอง 3 คน ไม่ได้ทำเพื่อส่งผู้ตรวจ (`docs/PROJECT.md` ข้อ 12)
หลักตัดสินใจ: ฟีเจอร์เพื่อ "พิสูจน์ว่าข้อมูลถูก" ไม่ต้องทำ · เพื่อ "ลดการพิมพ์ซ้ำ" ทำ

เพิ่มชนิดบล็อกใหม่: เขียนคอมโพเนนต์ → เพิ่ม `case` ใน `SlideRenderer.tsx`
→ ใส่ชื่อชนิดลงใน `IMPLEMENTED` ที่ `lib/deck.ts`

`list-table` ทำตารางแผนงานได้แล้ว — คอลัมน์ตั้ง `merge: true` รวมช่องที่ค่าซ้ำติดกัน ·
`kind: "months"` เป็นแถบ 12 เดือน (ค่าในแถวเป็น `[เดือนเริ่ม, เดือนจบ]`) ·
`rowsPerSlide` ตั้งจำนวนแถวต่อหน้าเองได้ (ค่าเริ่มต้น 9 · `t-clean-plan` ใช้ 22)
ของเดิมกาง 12 เดือน × 5 สัปดาห์ = 60 ช่องเพื่อบอกแค่ "ทำทั้งปี" · ย่อเหลือ 12 ช่องแล้ว

## เว็บขึ้นแล้ว — deploy อัตโนมัติ

**https://ttharkesm-admin01.github.io/TKF-Report/**

push ขึ้นบรานช์นี้ → `.github/workflows/deploy.yml` build แล้ว deploy เอง ไม่ต้องสั่งอะไร
(Pages ถูกเปิดด้วยมือไปแล้ว · `enablement: true` ใช้ไม่ได้ GITHUB_TOKEN สิทธิ์ไม่ถึง)

- URL อยู่ใต้ `/TKF-Report` ไม่ใช่รากโดเมน · `basePath` ตั้งผ่าน `PAGES_BASE_PATH` เฉพาะตอน CI
  รูปใน `public/` ต้องเติมคำนำหน้าเอง (ทำใน `lib/photos.ts`) เพราะใช้ `<img>` ไม่ใช่ `next/image`
- **รีโปนี้เป็น public และเว็บก็เป็นสาธารณะ** ใครมี URL ก็เปิดได้ ตัวเลขค่าใช้จ่ายอยู่บนนั้นหมด
  ผู้ใช้รับทราบแล้ว · ถ้าวันหนึ่งต้องกั้น ต้องย้ายไปเช่น Cloudflare Pages + Access
- **ห้าม commit ข้อมูลส่วนบุคคลรายคน** — วันลา เงินเบี้ยเลี้ยง เงินเดือน ที่ผูกกับชื่อ-สกุลจริง
  git เก็บ history ลบทีหลังก็ยังตามอ่านได้ · เคยตัดออก 3 ตารางจากเด็ค 1.x ด้วยเหตุนี้
  ชื่อคนในบริบทงาน (ผู้รับผิดชอบ ผู้ตรวจสอบ ผู้เข้าประชุม) ถือว่าโอเค
- **รูปไม่ได้อยู่ใน git** สไลด์รูปบนเว็บจึงว่างจนกว่าจะมีคนลากรูปใส่ `/arrange`

## skill ที่มีในโปรเจกต์

- **`verify`** — วิธีพิสูจน์ว่าการแก้ทำงานจริง (ตรวจล้นกรอบ แคปดู ทดสอบหน้าที่ commit ด้วย API ปลอม)
  **อ่านก่อนแก้เลย์เอาต์สไลด์หรือตัวคำนวณทุกครั้ง**
- **`ui-check`** — ตรวจหน้าที่ไม่ใช่สไลด์ (`/` `/edit` `/arrange` `/structure`) หลังแก้หน้าตา
  โฟกัสหลุดตอนพิมพ์ · คีย์บอร์ดตันตรงหัวข้อที่ย่อ · แถบ sticky เหลื่อม · จอแคบ
  ทุกข้อในนั้นคือบั๊กที่เคยเกิดจริงโดยที่ `npm run build` ผ่านหมด
- **`table-from-image`** — ถอดตารางจากภาพเข้า `sections.json` (เด็คเดิมหลายตารางเป็นรูป ดึงด้วยสคริปต์ไม่ได้)
  ถามผู้ใช้ก่อนเสมอว่าจะทำเองที่ `/structure` หรือให้เราถอด
- **`round`** — ขึ้นรอบเดือนใหม่ และรอบเดือนหนึ่งผู้ใช้ทำอะไรบ้าง
- **`scrutinize`** — รีวิวแบบคนนอก ตั้งคำถามว่าควรทำสิ่งนี้ไหมก่อน แล้วไล่โค้ดจริงว่าทำได้อย่างที่อ้าง
  ผู้ใช้ขอให้ลงไว้ตรวจโปรเจกต์นี้ · คัดลอกมาทั้งดุ้นจาก
  [thananon/9arm-skills](https://github.com/thananon/9arm-skills) (`skills/engineering/scrutinize`, commit `a1fc303`)
  **ห้ามแก้เนื้อในไฟล์** ถ้าต้องอัปเดตให้ดึงจากต้นทางใหม่ทับ

## กฎที่ห้ามพลาด

รายละเอียดอยู่ใน `docs/PROJECT.md` ข้อ 4 · สามข้อที่พังบ่อยที่สุด:

1. **เลขหัวข้อสร้างจากลำดับ array ใน `config/sections.json` เท่านั้น** ห้าม hardcode ที่อื่นเด็ดขาด
   ชุดที่ล็อกแล้ว: สวน = 2.3 · ของเสีย = 2.8 · ค่าใช้จ่าย = 2.9 · จบที่ 2.13
2. **Gemini ห้ามแตะตัวเลข** โค้ดคำนวณเสร็จแล้วส่ง `TrendFacts` ให้เรียบเรียงเป็นประโยคเท่านั้น
   fallback ที่โค้ดประกอบเองมีแล้วที่ `summarySentence()` — ตอนนี้เด็คใช้ตัวนี้ล้วน ยังไม่ได้ต่อ Gemini
3. **% และราคาต่อหน่วยห้ามบวกกัน** ใช้ `agg` จาก `UNITS` เสมอ อย่าเขียนสูตรรวมเอง
4. **หน้าที่เขียนไฟล์กลับเข้ารีโป ต้องอ่านของสดจาก GitHub ก่อนเสมอ**
   `config/*.json` ที่ `import` เข้ามาเป็นภาพนิ่งของตอน build ใช้เป็นฐานเขียนทับไม่ได้
   ไม่งั้นสิ่งที่คนอื่น (หรือตัวเราเองเมื่อนาทีก่อน) ส่งไปจะหายเงียบ ๆ — เคยเกิดจริงแล้ว
   ประกอบไฟล์ใน `getFiles(token)` ตอนกดส่ง **ห้ามประกอบตอน render**
   **กฎนี้กินทั้ง "ค่า" และ "โครง"** — คีย์แถว รายชื่อไฟล์รูป ลำดับ ต้องมาจากของสดด้วย
   เคยแก้เฉพาะค่าแล้วเหลือ `freeKey()` ตั้งคีย์จากของตอน build → ได้ `custom-1` สองแถว
   แก้ช่องเดียวเขียนลงทั้งคู่ถาวร · และ `/arrange` เขียน `arrange.json` ทับทั้งก้อน
   → คำบรรยายของรูปที่ยังไม่ทัน build หายและรูปที่ซ่อนไว้โผล่กลับ
   **อ่านของสดตั้งแต่เปิดหน้า ไม่ใช่แค่ตอนกดส่ง** ไม่งั้นตัวเตือน "กำลังลบตัวเลขเดิม"
   จะเทียบกับค่าตอน build แล้วเงียบทั้งที่ควรฟ้อง

ค่าในช่องตารางมี 3 สถานะ อย่ายุบรวมกัน — `number` มีค่า · `'none'` ไม่มีรายการ (แสดง `-`) ·
`null` ยังไม่กรอก (แสดงว่าง) ของเดิมใช้ `-` ปนกันหมดจึงแยกไม่ออกว่าลืมหรือไม่มี
ไม่ได้เก็บไว้เพื่อ audit แต่เพื่อให้เดือนหน้ารู้ว่าต้องตามเก็บข้อมูลหรือไม่ต้อง

## ข้อจำกัดสำคัญ: เครื่องคอมพิวเตอร์บริษัท

ผู้ใช้งานจริงใช้เครื่องของบริษัท → **เครื่องนั้นต้องใช้แค่เบราว์เซอร์ ห้ามต้องติดตั้งอะไรเลย**
พัฒนาบนเครื่องอื่น เครื่องบริษัทเปิดผ่าน URL อย่างเดียว

ยังไม่ได้ยืนยันกับ IT — ดูรายการที่ต้องถามใน `docs/PROJECT.md` ข้อ 10
**ตอนนี้ไม่มีส่วนไหนพึ่ง Drive หรือ Gemini แล้ว** รูปอ่านจากโฟลเดอร์ธรรมดา (ข้อ 11)
Drive เป็นแค่ทางเลือกหนึ่งในการพารูปมาลงโฟลเดอร์ · **ห้ามเขียนโค้ดที่ทำงานไม่ได้ถ้าไม่มี Drive**

## ค้างอยู่

- 🔴 **ข้อมูลเดือน ก.ค. ตอนนี้มีของทดสอบปนอยู่ ยังไม่ได้เก็บกวาด**
  commit `4e29a99` ใส่ `111` ลง 23 ช่องรวด และเพิ่มแถวชื่อ `กกกกกกก` (`custom-1`, 55555) ในตาราง OP
  เป็นการทดสอบหน้า `/edit` ไม่ใช่ตัวเลขจริง · **ตอนนี้ขึ้นบนเว็บสาธารณะเป็นตัวเลขจริง ๆ**
  รวมถึง `hazard-sell` ก.ค. ที่ของจริงคือ **2155** (หลุดเป็น `null` ตอน `fff77a5` แล้วถูกทับเป็น 111)
  ค่าเดิมทั้งหมดอยู่ใน git · ก่อน `4e29a99` และ `fff77a5` คือของจริง
  **ถามผู้ใช้ก่อนว่าจะย้อนหรือจะพิมพ์ทับเอง อย่าตัดสินใจแทน**
- ยังไม่มีรูปจริงสักใบ — ผู้ใช้ลากใส่หน้า `/arrange` ได้เองแล้ว ไม่ต้องผ่านเรา
  (หรือวางไฟล์ที่ `public/photos/2569-07/<รหัสบล็อก>/` แล้วสั่ง `npm run photos` ถ้ามีโค้ดในเครื่อง)
- **โค้ดโปรเจกต์ไม่ได้อยู่บนเครื่องผู้ใช้** อยู่ในคอนเทนเนอร์ของ Claude Code เท่านั้น
  อย่าออกแบบอะไรที่ต้องให้ผู้ใช้รันคำสั่งเอง — เขาใช้ได้แค่เบราว์เซอร์
- `driveFolderId` ยังว่างใน `g-garden-r1`, `g-garden-r2`, `s-appsheet` — เป็นข้อมูลประกอบ ระบบไม่ได้ใช้
- ยังไม่ได้ยืนยันนโยบาย IT ทั้ง 5 ข้อ
- `list-table` มี 8 บล็อก · **`t-clean-plan` มีข้อมูลจริงครบแล้ว** (22 แถว ถอดจากภาพ ผู้ใช้ยังไม่ได้ตรวจ)
  `t-vehicle` มีคอลัมน์แต่ `rows` ว่าง · อีก 6 บล็อกยังไม่มี `columns`
  (`t-garden-plan` `t-maint-report` `t-grease` `t-ticket` `t-risk` `t-audit`)
  **แต่ผู้ใช้กำหนดเองได้แล้วที่ `/structure` ไม่ต้องรอเรา** — ถามก่อนว่าจะทำเองหรือให้เราถอดจากภาพ
- **ข้อจำกัดของหน้า `/edit` ที่รู้แล้วและยอมรับไว้** (ไม่ใช่บั๊ก อย่าเพิ่งไปแก้)
  ตัวเตือน "ตัวเลขดูผิดปกติ" เตือนเฉพาะช่องที่แก้ในเซสชันนั้น ไม่ย้อนตรวจของเดิม ·
  การ์ดความคืบหน้าบนหน้าแรกเป็นค่าตอน build (แถบเดือนใน `/edit` ต่างหากที่เป็นของสด) ·
  จอแคบกว่า ~1240px ตารางแต่ละอันเลื่อนแนวนอนแยกกัน คอลัมน์เดือนจะเยื้องกันระหว่างตาราง
- **ยังไม่ได้รับไฟล์ 2.x** (`งานบริการสำนัก_7_69.pptx` 111 สไลด์ 161 MB) ซึ่งเป็นที่อยู่ของตาราง 6 อันนั้น
  ผู้ใช้ส่งมาแล้วแต่หมวด 1.x, 3.x, 4.x เท่านั้น — ดึงตารางเก็บไว้ที่ `docs/source/tables-2569-07.md`
- **ตารางในเด็คเดิมหลายอันเป็นรูปภาพ ไม่ใช่ตาราง PowerPoint จริง** ดึงด้วยสคริปต์ไม่ได้
  ต้องให้ผู้ใช้ส่งภาพมาแล้วถอดเอา (แบบที่ทำกับ `t-clean-plan`)
- สีชุดข้อมูลกราฟมี 5 สีที่ผ่านการตรวจตาบอดสีแล้ว เกินกว่านั้นระบบยุบเป็น "อื่น ๆ" ห้ามเพิ่มสีเอง
- `docs/PROJECT.md` ข้อ 8 เขียนว่าขั้นที่ 1 ได้ 29 สไลด์ แต่ config จริงมีปกแค่ 13 อัน
  เพราะเด็คเดิมมีปกย่อยรายหัวข้อย่อยด้วย · ถ้าต้องการปกย่อยจริงต้องเพิ่มบล็อก `cover` ใน `sections.json`
- `note` ใน `sections.json` 3 จุดเป็นข้อสังเกตที่รอผู้ใช้ตรวจสอบ ไม่ใช่ข้อสรุป
  (ขี้เถ้าบอยเลอร์ ก.พ. เป็น 0 · ค่าบำรุงสมาคมจ่าย 2 ครั้ง · ของเสียอันตรายกำจัดเป็น 0 ทั้งปี)

## การทำงาน

- **ตอบเป็นภาษาไทย** ผู้ใช้สื่อสารภาษาไทย
- **ผู้ใช้แคร์เรื่องโทเคน** ตอบตรงประเด็น อย่าสำรวจทางเลือกที่ไม่ได้จะทำ อย่าแคปหน้าจอถ้าไม่จำเป็น
  เปลี่ยนเรื่องเมื่อไรให้เริ่มเซสชันใหม่ ถูกกว่าคุยยาวในเซสชันเดียว
- ใช้ Next.js 15 (App Router) + React 19 + Tailwind v4 · `output: export` เป็นไฟล์นิ่ง
- แก้เลย์เอาต์สไลด์แล้วต้องตรวจตาม skill `verify` เสมอ — build ผ่านไม่ได้แปลว่าสไลด์ถูก
- **ห้ามใช้ `loading="lazy"` กับรูปในสไลด์** หน้า `/deck` คือทางออก PDF รูปที่ยังไม่โหลดจะพิมพ์เป็นช่องว่าง
- **ห้ามอัป TypeScript เป็น 7** — Next 15 โหลด `next.config.ts` ไม่ได้ ต้องอยู่ที่ 5.x
- **`claude/tkf-report-setup-kd6ejg` คือ default branch** และเป็นตัวที่ deploy เว็บ
  งานใหม่แยกบรานช์แล้วเปิด PR เข้าบรานช์นี้ (PR #2 กับ #3 merge ไปแล้วแบบนั้น)
  **ไม่มี CI รันบน PR** — `deploy.yml` ยิงเฉพาะตอน push เข้า default branch
  จึงต้องรัน `tsc` / `next build` / `check-overflow` เองก่อนเสมอ PR ที่ขึ้นเขียวไม่ได้แปลว่าตรวจแล้ว
- push ไฟล์ใน `.github/workflows/` อาจถูกบล็อก ต้องให้ผู้ใช้อนุมัติหรือ push เอง
