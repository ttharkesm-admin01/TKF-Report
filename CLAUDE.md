# CLAUDE.md

> **อ่าน `docs/PROJECT.md` ให้จบก่อนเริ่มงานทุกครั้ง** ไฟล์นี้เก็บเฉพาะสิ่งที่ต้องรู้ทันที
> รายละเอียดทั้งหมดอยู่ใน `docs/PROJECT.md` — ห้ามคัดลอกเนื้อหามาซ้ำที่นี่

## โปรเจกต์นี้คืออะไร

ระบบทำรายงานประจำเดือนของ **แผนกบริการสำนักงาน โรงงานผลิตอาหารสัตว์ธารเกษม (CPF)**
แทนการก๊อปวางเข้า PowerPoint ทุกเดือน · ปลายทางคือ **นำเสนอบนเว็บ (Next.js)** พิมพ์ PDF จากหน้าเดิม

**ขอบเขตเฟสแรก: หมวด 2.x เท่านั้น** (13 หัวข้อ) อีก 3 หมวดเป็นของหน่วยงานอื่น

## สถานะปัจจุบัน

ยังเป็น **โครงเปล่า** — มีแต่ config กับ type ยังไม่มีโค้ดแอปเลยสักบรรทัด

| ไฟล์ | หน้าที่ |
|---|---|
| `config/sections.json` | หัวใจของระบบ · 13 หัวข้อ + ข้อมูลจริงถึง ก.ค. 2569 |
| `shared/schema.ts` | type ของบล็อก + ตารางหน่วย 10 หน่วย + ตัวช่วยคำนวณ |
| `docs/PROJECT.md` | บริบท การตัดสินใจที่ล็อกแล้ว ลำดับการสร้าง 9 ขั้น |

**งานถัดไป: ขั้นที่ 1** — สร้าง `cover` + `closing` จาก config (29 สไลด์ ได้ฟรี)
ลำดับขั้นทั้งหมดอยู่ใน `docs/PROJECT.md` ข้อ 8

## กฎที่ห้ามพลาด

รายละเอียดอยู่ใน `docs/PROJECT.md` ข้อ 4 · สามข้อที่พังบ่อยที่สุด:

1. **เลขหัวข้อสร้างจากลำดับ array ใน `config/sections.json` เท่านั้น** ห้าม hardcode ที่อื่นเด็ดขาด
   ชุดที่ล็อกแล้ว: สวน = 2.3 · ของเสีย = 2.8 · ค่าใช้จ่าย = 2.9 · จบที่ 2.13
2. **Gemini ห้ามแตะตัวเลข** โค้ดคำนวณเสร็จแล้วส่ง `TrendFacts` ให้เรียบเรียงเป็นประโยคเท่านั้น
   และต้องมี fallback ที่โค้ดประกอบประโยคเองเมื่อไม่มี API key
3. **% และราคาต่อหน่วยห้ามบวกกัน** ใช้ `agg` จาก `UNITS` เสมอ อย่าเขียนสูตรรวมเอง

ค่าในช่องตารางมี 3 สถานะ อย่ายุบรวมกัน — `number` มีค่า · `'none'` ไม่มีรายการ (แสดง `-`) ·
`null` ยังไม่กรอก (แสดงว่าง + validate ไม่ผ่าน) ของเดิมใช้ `-` ปนกันหมดจึงแยกไม่ออกว่าลืมหรือไม่มี

## ข้อจำกัดสำคัญ: เครื่องคอมพิวเตอร์บริษัท

ผู้ใช้งานจริงใช้เครื่องของบริษัท → **เครื่องนั้นต้องใช้แค่เบราว์เซอร์ ห้ามต้องติดตั้งอะไรเลย**
พัฒนาบนเครื่องอื่น เครื่องบริษัทเปิดผ่าน URL อย่างเดียว

ยังไม่ได้ยืนยันกับ IT — ดูรายการที่ต้องถามใน `docs/PROJECT.md` ข้อ 10
**อย่าออกแบบให้ทุกอย่างพึ่ง Drive หรือ Gemini** ขั้นที่ 1–5 (ปก ตาราง กราฟ) ต้องทำงานได้โดยไม่มีทั้งสองอย่าง

## ค้างอยู่

- `driveFolderId` ยังเป็นค่าว่างใน `g-garden-r1`, `g-garden-r2`, `s-appsheet` — รอ ID จริงหลังตั้ง Google Form
- ยังไม่ได้ยืนยันนโยบาย IT ทั้ง 5 ข้อ
- `note` ใน `sections.json` 3 จุดเป็นข้อสังเกตที่รอผู้ใช้ตรวจสอบ ไม่ใช่ข้อสรุป
  (ขี้เถ้าบอยเลอร์ ก.พ. เป็น 0 · ค่าบำรุงสมาคมจ่าย 2 ครั้ง · ของเสียอันตรายกำจัดเป็น 0 ทั้งปี)

## การทำงาน

- **ตอบเป็นภาษาไทย** ผู้ใช้สื่อสารภาษาไทย
- พัฒนาบนบรานช์ `claude/tkf-report-setup-kd6ejg`
- รีโปนี้เริ่มจากว่างเปล่า บรานช์นี้จึงเป็น default branch — ยังไม่มีบรานช์ฐานให้เปิด PR
